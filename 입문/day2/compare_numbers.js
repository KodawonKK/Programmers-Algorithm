function solution(num1, num2) {
  if (num1 === num2) {
    return 1;
  } else {
    return -1;
  }
}

//삼항연산자 사용
function solution(num1, num2) {
  var answer = num1 === num2 ? 1 : -1;
  return answer;
}
